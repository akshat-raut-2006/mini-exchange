server:
  port: 8080

spring:
  datasource:
    url: jdbc:sqlite:api-server/data/exchange.db
    driver-class-name: org.sqlite.JDBC
  sql:
    init:
      mode: always
      schema-locations: classpath:schema.sql

logging:
  level:
    com.matchingengine: INFO
